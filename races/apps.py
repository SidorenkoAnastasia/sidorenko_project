from django.apps import AppConfig


class RacesConfig(AppConfig):
    # Конфигурация приложения для обработки гонок.
    # Этот класс определяет настройки, связанные с приложением 'races'
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'races'
